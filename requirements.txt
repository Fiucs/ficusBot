# FicusBot 项目依赖
# 生成时间: 2026-02-22

# 核心依赖 (来自 README.MD)
pyyaml==6.0.3
python-frontmatter==1.1.0
jsonschema==4.26.0
pyjson5==2.0.0
litellm==1.81.10
fastapi==0.129.0
uvicorn==0.40.0
colorama==0.4.6


# FastAPI 必需的依赖
pydantic==2.12.5

# MCP 功能依赖
mcp>=1.0.0

# 浏览器自动化依赖
browser-use>=0.1.0
playwright>=1.40.0

# 机器人平台依赖
python-telegram-bot>=21.0
websockets>=12.0

# 记忆系统依赖
lancedb>=0.3.0
sentence-transformers>=2.2.0

# 可选依赖 (增强功能)
# h11==0.14.0  # uvicorn 的依赖，会自动安装
# starlette>=0.27.0  # fastapi 的依赖，会自动安装
